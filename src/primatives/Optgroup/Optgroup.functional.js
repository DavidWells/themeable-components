import createFunctionalComponent from '../../utils/createFunctionalComponent'
import config from './Optgroup.config'

const Optgroup = createFunctionalComponent('Optgroup', config)
export default Optgroup
