import createClassComponent from '../../utils/createClassComponent'
import config from './Blockquote.config'

const Blockquote = createClassComponent('Blockquote', config)
export default Blockquote

