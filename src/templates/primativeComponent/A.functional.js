import createFunctionalComponent from '../createFunctionalComponent'
import config from './{{name}}.config'

const {{name}} = createFunctionalComponent('{{name}}', config)
export default {{name}}
